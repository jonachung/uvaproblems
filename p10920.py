def createGrid(lenGrid):
    for 


def main():
    numbers = input()
    while numbers != "0 0":
        num = numbers.split()
        lenGrid = int(num[0])
        position = int(num[1])
        grid = createGrid(lenGrid)
